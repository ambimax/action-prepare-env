{
    "name": "action-prepare-env",
    "packageManager": "yarn@3.1.0",
    "private": true,
    "scripts": {
        "dev": "esbuild --watch --bundle --minify --platform=node src/main.ts --outfile=dist/main.js",
        "build": "esbuild --bundle --minify --platform=node src/main.ts --outfile=dist/main.js",
        "test": "jest",
        "check": "tsc",
        "release": "semantic-release",
        "generate": "node -r esbuild-register scripts/generate"
    },
    "devDependencies": {
        "@actions/core": "^1.6.0",
        "@semantic-release/exec": "^6.0.2",
        "@semantic-release/git": "^10.0.1",
        "@types/common-tags": "^1",
        "@types/jest": "^27.0.3",
        "@types/node": "^14",
        "@types/prettier": "^2",
        "common-tags": "^1.8.2",
        "esbuild": "^0.13.15",
        "esbuild-jest": "^0.5.0",
        "esbuild-register": "^3.2.0",
        "jest": "^27.3.1",
        "node-dev": "^7.1.0",
        "prettier": "^2.5.0",
        "semantic-release": "^18.0.1",
        "typescript": "^4.5.2"
    }
}
